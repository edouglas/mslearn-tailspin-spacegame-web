trigger:
- '*'

pool:
  vmImage: 'ubuntu-18.04'
  demands:
  - npm

variables:
  buildConfiguration: 'Release'
  wwwrootDir: 'Tailspin.SpaceGame.Web/wwwroot'
  dotnetSdkVersion: '3.1.300'
  System.Debug: true

steps:
- task: UseDotNet@2
  displayName: 'Use .NET Core SDK $(dotnetSdkVersion)'
  inputs:
    version: '$(dotnetSdkVersion)'

- task: AzureKeyVault@1
  inputs:
    azureSubscription: 'Visual Studio Enterprise – MPN(d31ac6d8-ac40-408d-9925-eae1a20c99b7)'
    KeyVaultName: 'TestKeyVault-Alterra'
    SecretsFilter: '*'
- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: |
      Write-Host "$testvariable"
      Write-Host "$($test123.ToCharArray() -join ' ' )"
      $testvariable = "$test123"
      Write-Host "$testvariable"